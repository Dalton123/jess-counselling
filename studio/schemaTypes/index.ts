import {hero} from '../schemas/objects/hero'
import {page} from '../schemas/page'
import {service} from '../schemas/service'
import {services} from '../schemas/objects/services'
import {feature} from '../schemas/objects/feature'
import {sectionHeader} from '../schemas/objects/sectionHeader'
import {footer} from '../schemas/footer'
import {contact} from '../schemas/objects/contact'
import {logoShowcase} from '../schemas/objects/logoShowcase'

export const schemaTypes = [
  services,
  hero,
  page,
  service,
  feature,
  sectionHeader,
  footer,
  contact,
  logoShowcase,
]
